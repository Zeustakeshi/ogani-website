import React from 'react';

interface SortBarProps {}

const SortBar: React.FC<SortBarProps> = (props) => {
  return (
    <div className="sortbar-component">
      {/* TODO: Implement SortBar */}
    </div>
  );
};

export default SortBar;
