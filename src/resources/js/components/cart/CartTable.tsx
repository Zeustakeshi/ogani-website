import React from 'react';

interface CartTableProps {}

const CartTable: React.FC<CartTableProps> = (props) => {
  return (
    <div className="carttable-component">
      {/* TODO: Implement CartTable */}
    </div>
  );
};

export default CartTable;
