import React from 'react';

interface CartIconProps {}

const CartIcon: React.FC<CartIconProps> = (props) => {
  return (
    <div className="carticon-component">
      {/* TODO: Implement CartIcon */}
    </div>
  );
};

export default CartIcon;
