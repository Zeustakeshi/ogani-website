import React from 'react';

interface ProductMetaProps {}

const ProductMeta: React.FC<ProductMetaProps> = (props) => {
  return (
    <div className="productmeta-component">
      {/* TODO: Implement ProductMeta */}
    </div>
  );
};

export default ProductMeta;
