import React from 'react';

interface ProductInfoProps {}

const ProductInfo: React.FC<ProductInfoProps> = (props) => {
  return (
    <div className="productinfo-component">
      {/* TODO: Implement ProductInfo */}
    </div>
  );
};

export default ProductInfo;
